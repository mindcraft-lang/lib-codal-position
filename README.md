# Position

A two-axis position value type for CODAL-based targets.

Add this library to a project's dependencies:

```
gh:mindcraft-lang/lib-codal-position@0.1.3
```

Then import from it:

```ts
import { } from "@lib/mindcraft-lang/lib-codal-position";
```
